export enum Modality {
  KITESURF = 'KITESURF',
  WINGFOIL = 'WINGFOIL',
  KITEFOIL = 'KITEFOIL',
  KITEWAVE = 'KITEWAVE',
  OTHER = 'OTHER',
}

export enum Condition {
  NEW = 'NEW',
  LIKE_NEW = 'LIKE_NEW',
  USED = 'USED',
  FOR_PARTS = 'FOR_PARTS',
}

export enum ListingStatus {
  DRAFT = 'DRAFT',
  ACTIVE = 'ACTIVE',
  PAUSED = 'PAUSED',
  SOLD = 'SOLD',
  EXPIRED = 'EXPIRED',
}

export enum VerificationLevel {
  BASIC = 'BASIC',
  VERIFIED = 'VERIFIED',
  PRO = 'PRO',
}

export enum SubscriptionPlan {
  FREE = 'FREE',
  PRO = 'PRO',
  BUSINESS = 'BUSINESS',
}

export enum PaymentType {
  COMMISSION = 'COMMISSION',
  SUBSCRIPTION = 'SUBSCRIPTION',
  FEATURED = 'FEATURED',
  WHATSAPP_BOOST = 'WHATSAPP_BOOST',
}

export enum PaymentStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
  REFUNDED = 'REFUNDED',
}

export enum SupportedLocale {
  PT = 'pt',
  EN = 'en',
  ES = 'es',
}
